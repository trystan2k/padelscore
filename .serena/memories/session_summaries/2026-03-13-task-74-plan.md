## Goal
Prepare a planning-only execution plan for Task 74: migrate `app.json` to Zepp OS v3 screen-family targets.

## Instructions
- Repository is migrating to Zepp OS v3, overriding older v1-only repo docs.
- Scope strictly to Task 74; do not implement code.
- Preserve Task 73 outputs already present in repo state.

## Discoveries
- `app.json` is still `configVersion: "v2"` with an `app` identity block, Zepp OS `1.0.x` runtime versions, and legacy `gtr-3` / `gts-3` targets.
- Current repo truth for identity/version is `appId` `1108585` and version `3.0.0` (`package.json`, `app.json`, and `utils/version.js` aligned).
- `app-side/` and `setting/` are already removed from the repo and should not be reintroduced in Task 74.
- The PRD and Taskmaster describe the v3 target structure differently, so implementation should validate the exact manifest syntax against official Zepp tooling/docs before editing.
- All eight expected page files exist on disk: index, setup, game, summary, history, history-detail, game-settings, settings.

## Accomplished
- Reviewed current manifest, Task 73 artifacts, PRD migration guidance, Taskmaster subtasks, version-coupled files, and page inventory.
- Produced a concrete Task 74 plan mapped to subtasks `74.1`-`74.5`.
- Wrote the plan to `docs/plan/Plan 74 Migrate app.json to v3 Screen-Family Targets.md`.

## Next Steps
- Implement the manifest migration in `app.json` using the written plan.
- Validate the final v3 manifest via Zepp tooling, `npm run build:all`, simulator smoke on the four target families, and `npm run complete-check`.

## Relevant Files
- `app.json` — manifest to migrate from v2 device targets to v3 screen-family targets
- `docs/plan/Plan 74 Migrate app.json to v3 Screen-Family Targets.md` — execution-ready plan for Task 74
- `.taskmaster/tasks/tasks.json` — source of Task 74 subtask definitions
- `docs/PRD-Zepp OS API Level 3.6 Migration for New App.md` — migration requirements and target families
- `package.json` — current version baseline (`3.0.0`)
- `utils/version.js` — app version constant aligned with `app.json`