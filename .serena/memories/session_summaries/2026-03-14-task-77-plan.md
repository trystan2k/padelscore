## Goal
Create a deepthink execution plan for Task 77 (Rebuild Screen Adaptation for Multi-Family Support).

## Instructions
- Keep scope focused on adaptation, not visual redesign.
- Preserve the existing UI on square devices as much as possible.
- Apply a minimal, consistent fixed top safe-area/status-bar reservation on `w390-s`.
- Prefer simple Zepp-runtime-safe changes.

## Discoveries
- `utils/screen-utils.js` currently hardcodes GTS 3 square assumptions via `deviceSource` and `390x450` detection.
- The layout system already propagates `safeTop` through `utils/layout-engine.js` and shared presets.
- All four family asset directories already exist under `assets/`.
- Most pages already resolve layout through shared presets and `getScreenMetrics()`, which makes a shared utility-first refactor practical.

## Accomplished
- Reviewed relevant memories from Tasks 41 and 74.
- Inspected manifest targets, screen utilities, layout engine/presets, page layout modules, tests, and asset directory structure.
- Wrote the execution plan to `docs/plan/Plan 77 Rebuild Screen Adaptation for Multi-Family Support.md`.

## Next Steps
- Implement the plan by refactoring `utils/screen-utils.js` first, then design tokens, then page/layout consumers, then tests and asset audit.
- Validate family detection assumptions early, especially whether runtime dimensions are family-distinguishing on-device.

## Relevant Files
- `docs/plan/Plan 77 Rebuild Screen Adaptation for Multi-Family Support.md` — implementation-ready plan
- `utils/screen-utils.js` — current screen-family/safe-top logic to replace
- `utils/design-tokens.js` — token scaling entry point
- `utils/layout-engine.js` — safeTop propagation layer
- `utils/layout-presets.js` — shared page schema construction
- `page/game/ui-binding.js` — highest-risk header/score layout consumer